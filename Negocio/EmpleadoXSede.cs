using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Datos;

namespace Negocio
{
    class EmpleadoXSede
    {
        //public static Entities context()
        //{
        //    return Datos.Context.context();
        //}

        //static int insertar(Datos.EmpleadoXSede empxsede)
        //{
        //    context().EmpleadoXSede.AddObject(empxsede);
        //    return context().SaveChanges();
        //}
        //static void modificar(Datos.EmpleadoXSede empxsede)
        //{
        //    context().EmpleadoXSede.Attach(context().EmpleadoXSede.Single(p => p.idEmpleado == empxsede.idEmpleado && p.idSede == empxsede.idSede)); 
        //    context().EmpleadoXSede.ApplyCurrentValues(empxsede);
        //    context().SaveChanges();
        //}

    }
}
